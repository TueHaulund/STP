import os

env = Environment(CC = 'g++',
                  CCFLAGS = '-Wall -Wextra -ansi -pedantic -std=c++11',
                  ENV = {'PATH' : os.environ['PATH']},
                  TOOLS = ['mingw'] if os.name == 'nt' else ['gnulink','gcc','g++','gas','ar'],
                  CPPPATH = ['./include'])

unittest = env.Program(target='unittest', source=['./test/unittest.cpp'])
Default(unittest)